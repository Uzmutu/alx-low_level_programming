#include <stdio.h>
#include <unistd.h>
#include "main.h"
/**
 * add - function that addition
 * @a: variable that is added to b
 * @b: variable that is added to a
 *
 * Description: addition two given integer and return the value
 *
 * Return: return value of a + b
 */
int add(int a, int b)
{
	return (a + b);
}
