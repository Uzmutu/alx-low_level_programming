#include "main.h"
/**
 * main - tests a function
 *
 * Description: test if positive_or_negative output normally with 0
 *
 * Return: 0
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
